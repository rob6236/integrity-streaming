// lib/firebaseAdmin.ts
// Placeholder: we'll wire real Firebase Admin later without breaking the build.

export async function verifyIdToken(_idToken: string): Promise<{ uid: string } | null> {
  // TODO: implement with firebase-admin once keys are set up
  return null;
}
